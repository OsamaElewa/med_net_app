import 'package:flutter/cupertino.dart';

class FinalReportView extends StatelessWidget {
  const FinalReportView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(
          child: Text('Final Report'),
        )
      ],
    );
  }
}
